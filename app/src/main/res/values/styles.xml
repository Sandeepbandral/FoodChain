<resources>

    <!-- Base application theme. -->

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">

        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>

    <style name="Theme.Transparent" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="colorPrimaryDark">@android:color/transparent</item>
    </style>

    <!-- For Fonts -->

    <!-- Bold Font -->
    <style name="MyCustomTextStyle.Bold" parent="android:TextAppearance">
        <item name="fontPath">fonts/robotomedium.ttf</item>
    </style>

    <!-- Regular Font -->
    <style name="MyCustomTextStyle.Regular" parent="android:TextAppearance">
        <item name="fontPath">fonts/robotoregular.ttf</item>
    </style>

    <!-- Light Font -->
    <style name="MyCustomTextStyle.Light" parent="android:TextAppearance">
        <item name="fontPath">fonts/robotolight.ttf</item>
    </style>


    <!-- Bold Italic Font -->
    <style name="MyCustomTextStyle.Bold.Italic" parent="android:TextAppearance">
        <item name="fontPath">fonts/Roboto-BoldItalic.ttf</item>
    </style>




    <!-- For Doalog Animation -->

    <style name="dialog_animation">
        <item name="android:windowEnterAnimation">@anim/push_slide_in_up</item>
        <item name="android:windowExitAnimation">@anim/push_slide_out_down</item>
    </style>




    <!-- Tab Layout -->

    <style name="customTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">@color/colorPrimary</item>
        <item name="tabIndicatorHeight">@dimen/_2sdp</item>
        <item name="tabPaddingStart">0dp</item>
        <item name="tabPaddingEnd">0dp</item>
        <item name="textAllCaps">false</item>
        <item name="android:dividerPadding">0dp</item>
        <item name="android:textSize">@dimen/_12ssp</item>
        <item name="android:textColor">@color/colorGreyHint</item>
        <item name="tabSelectedTextColor">@color/colorPrimary</item>
        <item name="android:divider">@color/colorGreyHint</item>
        <item name="tabBackground">@color/colorWhite</item>
    </style>



</resources>
